{
  "name": "ai-ddd-devtrack",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "bun run --cwd frontend build && bun run --cwd backend build",
    "check": "bun run lint && bun run typecheck",
    "check:fix": "oxfmt . && oxlint --fix . && bun run --cwd backend format",
    "diff2prompt": "diff2prompt",
    "format:oxfmt": "oxfmt .",
    "format:oxfmt:check": "oxfmt --check .",
    "lint": "bun run format:oxfmt:check && oxlint . && bun run --cwd backend lint",
    "lint:oxlint": "oxlint .",
    "test": "bun run --cwd backend test",
    "typecheck": "bun run --cwd frontend typecheck"
  },
  "devDependencies": {
    "diff2prompt": "^0.0.15",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0"
  },
  "packageManager": "bun@1.4.0"
}
